"use client"

// LiveVoiceRecognition stub: voice feature removed. Renders nothing.
export default function LiveVoiceRecognition({ onCapture }: { onCapture?: (blobUrl: string, features?: number[]) => void }) {
  return null
}
