"use client"

// Voice recognition feature removed. This stub keeps the module present
// so imports don't fail during builds. It intentionally renders nothing.
export function VoiceRecognitionPage(): null {
  return null
}

export default VoiceRecognitionPage
// This stub keeps the route/component available for builds but disables functionality.
